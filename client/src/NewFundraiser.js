import React, { useState, useEffect } from "react";

// コンポーネントを用意する。
const NewFundraiser = () => {
    useEffect (() => {

    }, []);

    return (
        <div>
            <h2>Create a New Fundraiser</h2>
        </div>
    );
}

export default NewFundraiser;