import React, { useState, useEffect } from "react";

// コンポーネントを用意する。
const Home = () => {
    useEffect (() => {

    }, []);

    return (
        <div>
            <h2>Home</h2>
        </div>
    );
};

export default Home;