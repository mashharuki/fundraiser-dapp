/**
 * METoken用のテストコード
 */

// METokenコントラクトを読み込んでインスタンス化する。
// const METokenContract = artifacts.require("METoken");
// METFaucetコントラクトを読み込んでインスタンス化する。
// const METFaucetContract = artifacts.require("METFaucet");

/**
 * デプロイ関連のテストコード
 */ 

/*
contract ("METoken & METFaucet Factory: deployment", () => {
    // デプロイ済みかチェックする。
    it ("has been deployde", async () => {
        const METokenFactory = METokenContract.deployed();
        const METFaucetFactory = METFaucetContract.deployed();
        assert(METokenFactory, "METoken factory was not deployed");
        assert(METFaucetFactory, "METFaucet factory was not deployed");
    });
});
*/
